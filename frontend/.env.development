# Auto-loaded by `next dev` only (not used in `next build`).
# Backend default from backend/src/server.js — keep one API port project-wide.
# 若本機 3001 已被占用（例如 NAS 上的 Nextcloud），請複製 .env.development.local.example
# 為 .env.development.local 並改用 3110，且後端改跑 npm run dev:api:3110（repo 根目錄）。
# （Windows 常態排除 TCP 3006–3105，勿選該區間內的埠，否則會 EACCES。）
NEXT_PUBLIC_API_BACKEND_PORT=3001
