# Copy to .env.development.local (gitignored) when port 3001 is taken — e.g. Nextcloud on NAS.
# Use 3110 (not 3011): Windows often reserves TCP 3006–3105 → bind EACCES inside that range.
# Must match the backend PORT (e.g. npm run dev:api:3110 from repo root).
NEXT_PUBLIC_API_BACKEND_PORT=3110
