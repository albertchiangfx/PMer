# Database
DB_NAME=studio_pm
DB_USER=studio
DB_PASSWORD=changeme_in_production

# App
PORT=80
# With docker-compose + nginx, prefer same-origin proxy.
NEXT_PUBLIC_API_URL=/api

# File uploads (TrueNAS path mapping)
# UPLOAD_DIR=/mnt/data/studio-pm/uploads
